name: Build

permissions:
  contents: write

on:
  push:
    branches:
      - master
    paths:
      - "src/AkburaDocs/**"
      - "src/Akbura.FeatureGallery/**"
      - ".github/workflows/build.yml"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Install WebAssembly workload
        run: dotnet workload install wasm-tools

      - name: Build
        working-directory: ./src/AkburaDocs
        run: dotnet build .

      - name: Assign default GitHub URL base path
        env:
          GH_TOKEN: ${{ secrets.GH_PAGES_TOKEN }}
        run: |
          custom_domain=$(gh api "/repos/${{ github.repository }}/pages" | jq -r '.cname // empty')
          echo "CNAME config detected: $custom_domain"

          repo_name=$(echo "${{ github.repository }}" | cut -d '/' -f 2)
          org_name=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')

          if [ ! -f "./src/AkburaDocs/wwwroot/CNAME" ] && [ -z "$custom_domain" ]; then
            echo "ssg_base_href=https://$org_name.github.io/$repo_name/" >> "$GITHUB_ENV"
          elif [ -n "$custom_domain" ]; then
            echo "Persist CNAME '$custom_domain' to ./src/AkburaDocs/wwwroot/CNAME"
            echo "$custom_domain" > ./src/AkburaDocs/wwwroot/CNAME
            echo "custom_domain=$custom_domain" >> "$GITHUB_ENV"
          fi

      - name: Install Tailwind CSS
        run: |
          mkdir -p /home/runner/.local/bin
          curl -o "/home/runner/.local/bin/tailwindcss" \
            -L "https://github.com/tailwindlabs/tailwindcss/releases/latest/download/tailwindcss-linux-x64"
          chmod +x /home/runner/.local/bin/tailwindcss

      - name: Build Tailwind
        working-directory: ./src/AkburaDocs
        run: npm run build

      - name: Prerender
        working-directory: ./src/AkburaDocs
        run: |
          site_base_href="${{ env.ssg_base_href }}"

          if [ -n "$site_base_href" ]; then
            echo "Prerendering for deployment to $site_base_href"
            dotnet run \
              --no-launch-profile \
              --AppTasks=prerender \
              --configuration Release \
              --environment Production \
              --BaseHref "$site_base_href"
          else
            echo "Prerendering for deployment to $(cat ./wwwroot/CNAME)"
            dotnet run \
              --no-launch-profile \
              --AppTasks=prerender \
              --configuration Release \
              --environment Production
          fi

      - name: Publish Feature Gallery
        run: |
          dotnet publish \
            ./src/Akbura.FeatureGallery/Akbura.FeatureGallery.Browser/Akbura.FeatureGallery.Browser.csproj \
            --configuration Release \
            --output "${{ runner.temp }}/feature-gallery"

      - name: Add Feature Gallery to site
        run: |
          mkdir -p ./src/AkburaDocs/dist/Gallery
          cp -a "${{ runner.temp }}/feature-gallery/wwwroot/." ./src/AkburaDocs/dist/Gallery/

          test -f ./src/AkburaDocs/dist/404.html
          test -f ./src/AkburaDocs/dist/Gallery/index.html
          test -d ./src/AkburaDocs/dist/Gallery/_framework

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GH_PAGES_TOKEN }}
          publish_dir: ./src/AkburaDocs/dist
          user_name: GitHub Action
          user_email: action@github.com
